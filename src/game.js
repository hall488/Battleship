const Game = () => {
  let human;
  let computer;

  const start = (team) => {
    //human = player(team);
    //computer = team == "Rebels" ? player("Rebels") : player("Empire");
  };

  return { start };
};

export default Game;
