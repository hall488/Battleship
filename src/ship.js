const Ship = (_nid, _x, _y, _rotated, _width, _height) => {
  const id = () => _id;
  const x = () => _x;
  const y = () => _y;
  const rotated = () => _rotated;
  const width = () => _width;
  const height = () => _height;
  return { id, x, y, rotated, width, height };
};

export default Ship;
